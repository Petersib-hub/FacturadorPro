@if(config('verifactu.sandbox.enabled'))
    <div class="alert alert-warning mb-3">
        <strong>SANDBOX activo:</strong> este entorno es de <u>pruebas</u>. Los datos pueden purgarse.
    </div>
@endif
